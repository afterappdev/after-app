import 'package:flutter/material.dart';

import '../../core/theme/app_theme.dart';

class IntroStyle {
  static const Color bg = Colors.white;
  static const Color purple = AppTheme.brand;
  static const Color purpleSoft = Color(0xFFF7A05A);
  static const Color destinationBg = Colors.white;
  static const Duration duration = Duration(milliseconds: 4800);
  static const String fontFamily = AppTheme.fontFamily;
}
