org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Windows: Kotlin incremental caches store plugin sources as paths relative to
# android/. That fails when Pub Cache is on C: and this project is on D:
# (RelocatableFileToPathConverter: "this and base files have different roots").
# Official Kotlin Gradle property for JVM; does not change app/Firebase logic.
kotlin.incremental=false
